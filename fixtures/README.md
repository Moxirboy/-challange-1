# Challenge 1 — fixtures

- `seed_ontology.json` — the existing ontology: entity types, attributes (literal-valued, with datatypes), and relationships (entity-valued, with a range type).
- `1_vendors.csv`, `2_product_catalog.csv`, `3_crm_export.csv` — the CSVs to ingest, **in this order**. Apply accepted ontology changes before moving to the next file.

See the challenge doc for the task, output format, and deliverables.

Treat everything in this bundle the way you'd treat a real customer's data and ontology: **don't assume any of it is perfect.**
